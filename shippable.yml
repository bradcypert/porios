language: clojure

lein:
  - lein2

before_script:
  - echo "CREATE ROLE dev WITH LOGIN PASSWORD 'dev'" | psql -U postgres
  - psql -c 'create database poriostest;' -U postgres

script:
  - cd app; lein test2junit
